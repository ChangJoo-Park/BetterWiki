import { Request, Response } from "express";

/**
 * @param request
 * @param response
 */
export async function serviceUpdateAction(request: Request, response: Response) {
  return response.json([]);
}
